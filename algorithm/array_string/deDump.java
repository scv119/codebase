public class deDup{
    public static void dedup(char[] str){
        if(str == null || str.length < 2)
            return;

        for ( int i = 0 ; i < str.length; i ++){
            
        }
    }
}
